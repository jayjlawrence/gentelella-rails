Description:
    Installs sample view and layout files into your project.
    Adds route for '/gentelella/<page>.html' to access the sample pages

Example:
    rails generate gentelella:install
or  rails generate gentelella:install true
     - to force overwrite
     - otherwise existing files will be untouched and sample file will have .gentelella extension

see lib/generators/gentelella/templates for the files that will be installed

